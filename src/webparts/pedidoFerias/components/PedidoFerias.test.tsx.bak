import * as React from 'react';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import '@testing-library/jest-dom';
import PedidoFerias from './PedidoFerias';
import { IPedidoFeriasProps } from './IPedidoFeriasProps';
import { sp } from '@pnp/sp/presets/all';

// Mock do PnP
jest.mock('@pnp/sp/presets/all');

// Mock das props
const mockProps: IPedidoFeriasProps = {
  description: 'Test description',
  isDarkTheme: false,
  environmentMessage: 'Test environment',
  hasTeamsContext: false,
  userDisplayName: 'Test User',
  context: {} as any
};

describe('PedidoFerias Component', () => {
  beforeEach(() => {
    // Limpar todos os mocks antes de cada teste
    jest.clearAllMocks();
  });

  test('deve renderizar o componente principal', () => {
    render(<PedidoFerias {...mockProps} />);
    
    expect(screen.getByText('Filtros')).toBeInTheDocument();
    expect(screen.getByText('Pedidos de Férias (8 itens)')).toBeInTheDocument();
  });

  test('deve exibir todos os filtros corretamente', () => {
    render(<PedidoFerias {...mockProps} />);
    
    // Verificar se os filtros estão presentes
    expect(screen.getByLabelText('👤 Colaborador:')).toBeInTheDocument();
    expect(screen.getByLabelText('📊 Status:')).toBeInTheDocument();
    expect(screen.getByPlaceholderText('🔍 Buscar por motivo...')).toBeInTheDocument();
  });

  test('deve exibir os pedidos de férias', () => {
    render(<PedidoFerias {...mockProps} />);
    
    // Verificar se os funcionários estão sendo exibidos
    expect(screen.getByText('João Silva')).toBeInTheDocument();
    expect(screen.getByText('Maria Santos')).toBeInTheDocument();
    expect(screen.getByText('Ana Costa')).toBeInTheDocument();
    expect(screen.getByText('Ricardo Oliveira')).toBeInTheDocument();
  });

  test('deve exibir os status corretos', () => {
    render(<PedidoFerias {...mockProps} />);
    
    expect(screen.getAllByText('PENDENTE')).toHaveLength(4);
    expect(screen.getAllByText('APROVADO')).toHaveLength(3);
    expect(screen.getAllByText('REJEITADO')).toHaveLength(1);
  });

  test('deve exibir botões de ação para pedidos pendentes', () => {
    render(<PedidoFerias {...mockProps} />);
    
    const approveButtons = screen.getAllByText('✓ Aprovar');
    const rejectButtons = screen.getAllByText('✗ Rejeitar');
    
    // Deve ter botões ativos para pedidos pendentes
    expect(approveButtons.length).toBeGreaterThan(0);
    expect(rejectButtons.length).toBeGreaterThan(0);
  });

  test('deve desabilitar botões para pedidos já processados', () => {
    render(<PedidoFerias {...mockProps} />);
    
    const allApproveButtons = screen.getAllByText('✓ Aprovar');
    const allRejectButtons = screen.getAllByText('✗ Rejeitar');
    
    // Verificar se alguns botões estão desabilitados
    const disabledApproveButtons = allApproveButtons.filter(button => 
      (button as HTMLButtonElement).disabled
    );
    const disabledRejectButtons = allRejectButtons.filter(button => 
      (button as HTMLButtonElement).disabled
    );
    
    expect(disabledApproveButtons.length).toBeGreaterThan(0);
    expect(disabledRejectButtons.length).toBeGreaterThan(0);
  });

  test('deve exibir motivo de rejeição para pedidos rejeitados', () => {
    render(<PedidoFerias {...mockProps} />);
    
    expect(screen.getByText(/Motivo da rejeição: Período de alta demanda de projeto/)).toBeInTheDocument();
  });

  test('deve ter opções corretas no filtro de colaborador', () => {
    render(<PedidoFerias {...mockProps} />);
    
    const collaboratorSelect = screen.getByLabelText('👤 Colaborador:');
    
    expect(collaboratorSelect).toHaveDisplayValue('Todos os colaboradores');
    
    // Verificar se as opções estão presentes
    const options = screen.getAllByRole('option');
    expect(options).toHaveLength(9); // Todos + 8 colaboradores
  });

  test('deve ter opções corretas no filtro de status', () => {
    render(<PedidoFerias {...mockProps} />);
    
    const statusSelect = screen.getByLabelText('📊 Status:');
    
    expect(statusSelect).toHaveDisplayValue('Todos os estados');
    
    // Verificar se as opções de status estão presentes
    expect(screen.getByDisplayValue('Todos os estados')).toBeInTheDocument();
  });

  test('deve ter controles de ordenação', () => {
    render(<PedidoFerias {...mockProps} />);
    
    expect(screen.getByText('Ordenar por:')).toBeInTheDocument();
    
    const sortSelect = screen.getByRole('combobox', { name: /ordenar/i });
    expect(sortSelect).toBeInTheDocument();
  });

  test('deve exibir períodos de férias formatados corretamente', () => {
    render(<PedidoFerias {...mockProps} />);
    
    // Verificar se os períodos estão sendo exibidos
    expect(screen.getByText('01/07/2025 — 15/07/2025 (15 dias)')).toBeInTheDocument();
    expect(screen.getByText('20/12/2025 — 30/12/2025 (10 dias)')).toBeInTheDocument();
  });

  test('deve exibir datas de solicitação formatadas', () => {
    render(<PedidoFerias {...mockProps} />);
    
    expect(screen.getByText('📅 Solicitado em: 15/06/2025 11:00')).toBeInTheDocument();
    expect(screen.getByText('📅 Solicitado em: 01/11/2025 14:30')).toBeInTheDocument();
  });

  test('deve ter todos os botões de detalhes', () => {
    render(<PedidoFerias {...mockProps} />);
    
    const detailsButtons = screen.getAllByText('👁 Detalhes');
    expect(detailsButtons).toHaveLength(8); // Um para cada funcionário
  });

  test('deve ter avatares únicos para cada funcionário', () => {
    render(<PedidoFerias {...mockProps} />);
    
    expect(screen.getByText('JS')).toBeInTheDocument(); // João Silva
    expect(screen.getByText('MS')).toBeInTheDocument(); // Maria Santos
    expect(screen.getByText('AC')).toBeInTheDocument(); // Ana Costa
    expect(screen.getByText('RO')).toBeInTheDocument(); // Ricardo Oliveira
  });

  test('deve aplicar classes CSS corretas para diferentes status', () => {
    render(<PedidoFerias {...mockProps} />);
    
    const pendingItems = screen.getByText('João Silva').closest('.pending');
    const approvedItems = screen.getByText('Maria Santos').closest('.approved');
    const rejectedItems = screen.getByText('Ricardo Oliveira').closest('.rejected');
    
    expect(pendingItems).toBeInTheDocument();
    expect(approvedItems).toBeInTheDocument();
    expect(rejectedItems).toBeInTheDocument();
  });
});