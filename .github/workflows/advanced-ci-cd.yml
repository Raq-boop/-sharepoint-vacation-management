name: Archived - advanced-ci-cd (moved)

# This workflow was archived and replaced with a lightweight pointer in ops/archived/workflows.
# Keeping a tiny file here prevents accidental re-activation. To restore the original workflow,
# copy the detailed workflow content from ops/archived or from the git history.