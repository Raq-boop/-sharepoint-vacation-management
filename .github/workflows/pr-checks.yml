name: Archived - pr-checks (moved)

# This workflow was archived and replaced with a lightweight pointer in ops/archived/workflows.
# Keeping this minimal file avoids running heavy PR checks. Restore original content from ops/archived if needed.